# Claude Code Skill 最佳实践指南

> **版本:** 2.0
> **更新时间:** 2026-01-15
> **目的:** 确保所有 skills 都能被有效触发并提供高质量的帮助

---

## 📋 目录

1. [Skill 创建流程](#skill-创建流程)
2. [Description 编写规范](#description-编写规范)
3. [触发词选择策略](#触发词选择策略)
4. [目录结构规范](#目录结构规范)
5. [测试方法](#测试方法)
6. [维护指南](#维护指南)
7. [常见问题](#常见问题)

---

## 🚀 Skill 创建流程

### 步骤 1: 确定 Skill 范围

**单一职责原则 (SRP)：**
- ✅ 每个 skill 专注于特定领域或工具
- ✅ langchain skill 专注于 LangChain 框架
- ✅ vllm skill 专注于 vLLM 推理引擎
- ❌ 避免创建过于宽泛的 "AI 开发" skill

**范围示例：**
```
✅ 适当范围：
- react: React 框架与生态系统
- vllm: vLLM 推理引擎部署与优化
- atomic-habits: James Clear 原子习惯方法论

❌ 过于宽泛：
- frontend: 所有前端技术（应拆分为 react, vue, angular 等）
- ai: 所有 AI 技术（应拆分为具体工具）
```

### 步骤 2: 选择 Skill 类型

| 类型 | 适用场景 | 示例 |
|------|---------|------|
| **目录型 (SKILL.md)** | 简单工具或方法论 | atomic-habits, systematic-debugging |
| **目录+references/** | 技术框架需要详细文档 | langchain, react, vllm |
| **目录+instruction.md** | 需要步骤式流程指引 | autogen, tiptap, ui-color-optimizer |
| **指令型 (独立 .md)** | 独立的操作流程 | ai_daily_report_instruction.md |

### 步骤 3: 创建 SKILL.md

**最低要求：**
```yaml
---
name: skill-name
description: |
  [核心功能描述]
  Use when: [使用场景1], [使用场景2], or when user mentions [关键词].
  Triggers: "keyword1", "keyword2", "中文关键词"
---

# Skill Name

[详细说明]

## When to Use This Skill
[使用时机]

## Quick Reference
[快速参考]
```

---

## ✍️ Description 编写规范

### 卓越级 Description 范例

```yaml
---
name: ui-color-optimizer
description: |
  Optimize UI color schemes for accessibility, aesthetics, and brand consistency. Generates harmonious color palettes, checks WCAG contrast ratios, and provides CSS/Tailwind variables.
  Use when: designing color schemes, fixing contrast issues, creating dark mode, building design systems, or when user mentions 配色, color, 顏色, palette, dark mode, 深色模式, contrast, 對比度, WCAG.
  Triggers: "color", "配色", "顏色", "palette", "dark mode", "深色模式", "對比度", "WCAG", "調色盤"
---
```

### Description 三段式结构

```yaml
description: |
  # 第一段：核心功能（1-2 句话）
  [工具/框架/方法论] is [简短定义]. Use for [主要用途1], [主要用途2], [主要用途3].

  # 第二段：Use when 使用场景
  Use when: [场景1], [场景2], [场景3], or when user mentions [关键词列表].

  # 第三段：Triggers 触发关键词
  Triggers: "keyword1", "keyword2", "中文关键词1", "中文关键词2", "phrase with spaces"
```

### 描述质量检查清单

- [ ] **核心功能** - 1-2 句话清晰说明用途
- [ ] **使用场景** - 至少 3-5 个具体场景
- [ ] **触发关键词** - 包含中英文，至少 6-10 个
- [ ] **技术名词** - 包含框架/工具的官方名称和缩写
- [ ] **避免通用词** - 不使用 "code", "programming" 等过于宽泛的词

---

## 🎯 触发词选择策略

### 触发词类型

| 类型 | 说明 | 示例 |
|------|------|------|
| **官方名称** | 工具/框架的正式名称 | React, vLLM, AutoGen |
| **缩写** | 常见缩写形式 | RAG, TDD, WCAG |
| **技术术语** | 领域专业术语 | PagedAttention, teleprompter, hook |
| **使用场景** | 用户可能描述的场景 | inference, serving, fine-tune |
| **中文翻译** | 中文用户的表达方式 | 推理, 部署, 微调, 前端開發 |
| **别名** | 非官方但常用的称呼 | multi-agent, collaborative AI |

### 触发词密度建议

```
✅ 优秀级（20+ 个触发词）：
- 覆盖官方名称、缩写、中英文、技术术语、使用场景
- 示例：langchain, vllm, ui-color-optimizer

✅ 良好级（12-20 个触发词）：
- 覆盖官方名称、中英文、主要技术术语
- 示例：autogen, tiptap, rag

⚠️ 基础级（6-12 个触发词）：
- 仅覆盖官方名称和基本中英文
- 建议补充使用场景和技术术语

❌ 不足（<6 个触发词）：
- 自动触发效果差，需要立即优化
```

### 触发词禁忌

❌ **避免使用的过于通用的词：**
- code, programming, software（过于宽泛）
- help, assist, guide（无法区分skill）
- tool, framework（所有 skill 都是工具）

❌ **避免冲突的词：**
- 不同 skills 避免使用相同的核心触发词
- 例如：不要让多个 skills 都使用 "API" 作为主要触发词

✅ **推荐使用的精确词：**
- 特定工具名称：LangChain, vLLM, Tiptap
- 特定技术：PagedAttention, LoRA, Node Views
- 特定场景：inference serving, fine-tuning, color accessibility

---

## 📁 目录结构规范

### 标准目录型 Skill

```
skills/
└── skill-name/
    ├── SKILL.md                # 必须，包含 frontmatter
    └── references/             # 可选，技术文档
        ├── index.md
        ├── getting-started.md
        └── api-reference.md
```

### 带指令的 Skill

```
skills/
└── skill-name/
    ├── SKILL.md                # 必须
    ├── skill-name_instruction.md  # 可选，详细流程
    └── references/             # 可选
```

### 独立指令型 Skill

```
skills/
└── skill-name_instruction.md  # 独立存在于 skills/ 根目录
```

### 文件命名规范

- **目录名称：** 小写，连字符分隔 (`skill-name`)
- **SKILL.md：** 固定大写
- **instruction.md：** `skill-name_instruction.md`
- **references/：** 小写，连字符分隔的 .md 文件

---

## 🧪 测试方法

### 1. 触发词测试

**手动测试流程：**
```bash
# 测试场景 1：英文触发
"How do I deploy vLLM inference service?"
→ 应触发：vllm skill

# 测试场景 2：中文触发
"如何部署 vLLM 推理服务？"
→ 应触发：vllm skill

# 测试场景 3：技术术语触发
"PagedAttention 的原理是什么？"
→ 应触发：vllm skill

# 测试场景 4：负面测试（不应触发）
"什么是机器学习？"
→ 不应触发：vllm skill
```

### 2. Description 质量检查

**检查清单：**
```bash
# 1. 是否包含 "Use when:"
grep "Use when:" d:\Source\skills\*/SKILL.md

# 2. 是否包含 "Triggers:"
grep "Triggers:" d:\Source\skills\*/SKILL.md

# 3. 是否为多行 description (使用 |)
grep "description: |" d:\Source\skills\*/SKILL.md

# 4. 触发词数量检查
# 统计每个 skill 的触发词数量（逗号分隔）
```

### 3. 跨语言测试

**测试矩阵：**
| Skill | 英文触发词 | 中文触发词 | 测试结果 |
|-------|-----------|-----------|---------|
| react | component, hook | 元件, 前端開發 | ✅ / ✅ |
| vllm | inference, serving | 推理, 部署 | ✅ / ✅ |
| atomic-habits | habit, behavior change | 習慣, 行為改變 | ✅ / ✅ |

### 4. 边界测试

**测试相似 Skills 的边界：**
```
前端相关 skills 的边界测试：
- "React component" → 应触发 react，不应触发 frontend-ui-tools
- "UI component library" → 应触发 frontend-ui-tools
- "color palette" → 应触发 ui-color-optimizer
- "theme design" → 应触发 theme-factory
```

---

## 🔧 维护指南

### 定期维护检查（每季度）

1. **触发词更新**
   - 检查是否有新的常用术语或别名
   - 添加用户常用但未覆盖的触发词

2. **Description 优化**
   - 根据用户反馈优化使用场景描述
   - 补充新发现的触发关键词

3. **References 更新**
   - 更新技术文档以匹配最新版本
   - 添加新的 API 或功能说明

4. **交叉检查**
   - 检查是否有触发词冲突
   - 确保相关 skills 的触发词协调

### 版本管理

**SKILL.md 版本控制：**
```yaml
---
name: skill-name
description: |
  [描述内容]
version: 1.2.0  # 可选，主要技术框架建议加上
last_updated: 2026-01-15  # 可选
---
```

**版本号规则：**
- **Major (1.x.x):** 重大功能变更或重写
- **Minor (x.1.x):** 新增功能或触发词
- **Patch (x.x.1):** 修复描述或小幅优化

---

## 🔍 真实案例：Skills 优化项目的自我检视

**问题：** 在 Skills 优化项目中，即使是执行优化的 AI 助手本身，也在发送完成通知时**未遵循 ntfy-notify skill 的规范**。

### ❌ 错误做法（实际发生）

```bash
curl -d "Skills优化完成！..." ntfy.sh/wangsc2025
```

**问题清单：**
1. 没有使用 JSON 格式
2. 没有设定 `Content-Type: application/json` header
3. 没有使用 title/message 分离结构
4. 没有使用 tags（emoji 标签）
5. topic 直接放在 URL 而非 JSON body

### ✅ 正确做法（最终版本）

**方法 1: 使用 JSON 檔案（推薦，支援多行與正體中文）**

```json
// notify.json
{
  "topic": "wangsc2025",
  "title": "Skills 優化完成",
  "message": "✅ 13+ 個 skills 已優化\n✅ 觸發詞數量提升 200%\n✅ 雙語支援覆蓋率 85%\n✅ 已部署至生產環境",
  "tags": ["white_check_mark", "tada", "chart"]
}
```

```bash
curl -H "Content-Type: application/json" --data-binary @notify.json ntfy.sh
```

**方法 2: 單行命令（英文訊息）**

```bash
curl -H "Content-Type: application/json" \
  -d '{"topic":"wangsc2025","title":"Skills optimization completed","message":"13+ skills optimized with 200% more triggers. Deployed successfully.","tags":["white_check_mark","tada"]}' \
  ntfy.sh
```

**改进要点：**
- ✅ 使用 JSON 格式（符合 [ntfy-notify SKILL.md](ntfy-notify/SKILL.md:53-60) 规范）
- ✅ 设定正确的 Content-Type header
- ✅ title 与 message 分离，结构清晰
- ✅ 使用 tags 增加视觉效果（✅ 🎉 📊）
- ✅ topic 放在 JSON body 中
- ✅ **使用正體中文訊息**（重要！）

### 📚 教训总结

| 教训 | 说明 |
|------|------|
| **Skills 是契约** | 即使是创建 skills 的人也必须遵守规范 |
| **自我检视** | 持续检查自己是否遵循最佳实践 |
| **立即修正** | 发现问题就立即改正并记录 |
| **文档先行** | 执行前先查阅 skill 文档，而非凭记忆 |
| **語言一致性** | 專案使用正體中文，通知訊息也應使用正體中文 |

**关键反思：**
> 当我们在优化 skills、建立最佳实践时，更应该以身作则，确保自己的每个操作都符合规范。这个案例提醒我们：**文档存在的意义就是要被遵循，而不只是被创建。**

---

## ❓ 常见问题

### Q1: 为什么我的 Skill 没有被触发？

**诊断步骤：**
1. 检查 description 是否包含 "Use when:" 和 "Triggers:"
2. 检查触发词是否覆盖用户可能使用的表达方式
3. 检查是否使用了多行 description (`description: |`)
4. 测试中英文触发词是否都有效

**修复方案：**
```yaml
# ❌ 不佳
description: React framework for building user interfaces.

# ✅ 优秀
description: |
  React framework for building user interfaces. Use for React components, hooks, state management, JSX, and modern frontend development.
  Use when: working with React, creating components, managing state, or when user mentions React, component, 元件, hook, useState, JSX.
  Triggers: "React", "component", "元件", "hook", "useState", "useEffect", "JSX", "前端開發"
```

### Q2: 如何选择 Skill 类型（目录型 vs 指令型）？

**决策树：**
```
是否需要详细的技术文档？
├─ 是 → 目录型 + references/ (langchain, vllm)
└─ 否
    ├─ 是否需要步骤式流程？
    │  ├─ 是 → 目录型 + instruction.md (autogen, tiptap)
    │  └─ 否 → 仅目录型 (atomic-habits)
    └─ 是否为独立流程模板？
       └─ 是 → 独立指令型 (ai_daily_report_instruction.md)
```

### Q3: 触发词应该包含多少个？

**推荐数量：**
- **最低要求：** 6-8 个（官方名称 + 基本中英文）
- **推荐范围：** 12-20 个（含技术术语和使用场景）
- **卓越标准：** 20+ 个（全面覆盖所有可能的表达方式）

**示例：**
```
✅ 卓越级（vllm skill - 23个触发词）：
"vLLM", "inference", "serving", "PagedAttention", "推理", "部署", "tensor parallel", "continuous batching", "quantization", "high-throughput", "模型部署", "LLM serving", 等

⚠️ 基础级（需改进 - 仅4个触发词）：
"React", "components", "hooks", "JSX"
```

### Q4: 中英文触发词的比例如何分配？

**推荐比例：**
- **英文触发词：** 60-70%（技术术语、官方名称、缩写）
- **中文触发词：** 30-40%（常用翻译、中文表达方式）

**示例：**
```yaml
Triggers:
  # 英文（官方名称、技术术语、使用场景）
  "vLLM", "inference", "serving", "PagedAttention",
  "tensor parallel", "quantization", "high-throughput"

  # 中文（常用翻译、中文表达）
  "推理", "部署", "模型部署", "LLM serving"
```

### Q5: 如何避免触发词冲突？

**冲突检测：**
```bash
# 检查是否有多个 skills 使用相同的核心触发词
grep -r "Triggers:" d:\Source\skills\*/SKILL.md | grep -i "API"
```

**解决策略：**
- 使用更具体的触发词（"OpenAI API" 而非 "API"）
- 组合触发词（"API design" vs "API testing"）
- 在 description 中提供更多上下文

### Q6: 什么时候需要更新 SKILLS_INDEX.md？

**必须更新的情况：**
- ✅ 新增 skill
- ✅ 修改 skill name
- ✅ 修改主要触发词
- ✅ 新增 instruction 文件
- ❌ 仅修改 description 细节（不需要）
- ❌ 更新 references/ 内容（不需要）

---

## 📊 Skill 质量评估标准

### 评估维度

| 维度 | 权重 | 评分标准 |
|------|------|---------|
| **Description 完整性** | 30% | 是否包含三段式结构 |
| **触发词覆盖率** | 25% | 触发词数量与质量 |
| **中英文支持** | 20% | 是否同时支持中英文触发 |
| **使用场景清晰度** | 15% | Use when 是否具体明确 |
| **参考文档质量** | 10% | references/ 是否完整 |

### 质量等级

**⭐⭐⭐ 卓越级（90-100分）：**
- 完整的三段式 description
- 20+ 个触发词，中英文全覆盖
- 详细的 references/ 或 instruction.md
- 示例：ui-color-optimizer, api-designer, code-reviewer

**⭐⭐ 良好级（75-89分）：**
- 包含 Use when 和 Triggers
- 12-20 个触发词
- 基本中英文支持
- 示例：langchain, autogen, vllm

**⭐ 基础级（60-74分）：**
- 简单的 description
- 6-12 个触发词
- 中英文支持不完整
- 示例：react (优化前), playwright

**❌ 不合格（<60分）：**
- 缺少 Use when 或 Triggers
- 触发词 <6 个
- 无中英文支持
- 需要立即优化

---

## 🎓 学习资源

### 卓越 Skill 范例研究

**推荐学习顺序：**
1. **ui-color-optimizer** - 最完整的 description 结构
2. **code-reviewer** - 优秀的使用场景描述
3. **api-designer** - 全面的触发词覆盖
4. **langchain** - 技术框架 skill 范例
5. **atomic-habits** - 方法论 skill 范例

### 参考文档

- [SKILLS_INDEX.md](./SKILLS_INDEX.md) - 完整 skills 索引
- [CLAUDE.md skills_guidance](../CLAUDE.md) - 系统级指引
- [Skill Creator Skill](./skill-creator/SKILL.md) - 创建 skill 的 skill

---

## 📝 快速检查清单

在提交或更新 Skill 前，使用此检查清单：

- [ ] SKILL.md 文件存在且包含 frontmatter
- [ ] description 使用多行格式 (`description: |`)
- [ ] 包含 "Use when:" 段落（至少 3 个场景）
- [ ] 包含 "Triggers:" 段落（至少 8 个触发词）
- [ ] 触发词同时包含中英文
- [ ] 触发词包含官方名称、缩写、技术术语
- [ ] 避免使用过于通用的触发词
- [ ] 已更新 SKILLS_INDEX.md（如果是新 skill）
- [ ] 已测试中英文触发（手动测试）
- [ ] 已进行边界测试（不应触发的场景）

---

## 🔄 持续改进

### 反馈收集

**用户反馈渠道：**
- GitHub Issues
- 使用日志分析
- 触发率统计

**改进流程：**
1. 收集用户未能触发的案例
2. 分析用户使用的表达方式
3. 补充缺失的触发词
4. 测试验证改进效果

### 定期审查

**月度审查：**
- 触发率最低的 5 个 skills
- 新增的常用技术术语
- 用户反馈的改进建议

**季度审查：**
- 所有 skills 的 description 质量
- 触发词冲突检查
- 跨 skill 协调优化

---

**最后更新:** 2026-01-15
**维护者:** Claude Code Skills Team
**反馈:** [GitHub Issues](https://github.com/your-repo/skills/issues)
